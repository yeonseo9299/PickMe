import { NextRequest, NextResponse } from 'next/server';
import { ObjectId } from 'mongodb';
import { getDatabase } from '@/lib/mongodb';

export async function PATCH(req:NextRequest,{params}:{params:Promise<{choiceId:string}>}){ try{ const {choiceId}=await params; const body=await req.json(); const {userId,name,category}=body; if(!userId)return NextResponse.json({message:'로그인이 필요합니다.'},{status:401}); if(!name?.trim())return NextResponse.json({message:'선택지를 입력해주세요.'},{status:400}); const db=await getDatabase(); const r=await db.collection('choices').updateOne({_id:new ObjectId(choiceId),userId},{$set:{name:name.trim(),...(category?{category}: {})}}); if(!r.matchedCount)return NextResponse.json({message:'선택지를 찾을 수 없습니다.'},{status:404}); return NextResponse.json({message:'수정되었습니다.'}); }catch{return NextResponse.json({message:'선택지 수정에 실패했습니다.'},{status:500});}}
export async function DELETE(req:NextRequest,{params}:{params:Promise<{choiceId:string}>}){ try{const {choiceId}=await params; const userId=new URL(req.url).searchParams.get('userId'); if(!userId)return NextResponse.json({message:'로그인이 필요합니다.'},{status:401}); const db=await getDatabase(); const r=await db.collection('choices').deleteOne({_id:new ObjectId(choiceId),userId}); if(!r.deletedCount)return NextResponse.json({message:'선택지를 찾을 수 없습니다.'},{status:404}); return NextResponse.json({message:'삭제되었습니다.'});}catch{return NextResponse.json({message:'선택지 삭제에 실패했습니다.'},{status:500});}}
